SELECT name, count(name)
FROM animal_ins
GROUP BY name
HAVING count(name) > 1
ORDER BY name;

# -- 코드를 입력하세요
# SELECT NAME, count(name) as COUNT
# FROM ANIMAL_INS
# GROUP BY NAME 
# HAVING count(name) >1
# ORDER BY NAME